# Images

- **`walking_pose_preview.png`** — three-frame pose skeleton preview (start / middle / end of `walking_sample_v1/data.jsonl`).

Regenerate locally:

```bash
pip install matplotlib
python scripts/render_pose_preview.py
```

Add your own screenshots (e.g. engine UI, quality report plots) here for the GitHub repo description and README.
